class Solution {
    public String frequencySort(String s) {
        StringBuilder ans=new StringBuilder();
        Map<Character,Integer> Store=new HashMap<>();
        for(char ch:s.toCharArray()){
            Store.put(ch,Store.getOrDefault(ch,0)+1);
        }
        List<Character> list=new ArrayList(Store.keySet());
        list.sort((ob1,ob2) -> Store.get(ob2)-Store.get(ob1));
        for(char ch:list){
            for(int i=0;i<Store.get(ch);i++){
                ans.append(ch);
            }
        }
    
        return ans.toString();

    }
}


Time Complexity: O(n log n) worst case, O(n) average (when k is small).

Space Complexity: O(n).
